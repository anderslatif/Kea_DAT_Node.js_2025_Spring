import { Router } from 'express';

const router = Router();

router.get("/pills", (req, res) => {
    res.send({ });
});

router.post("/pills", (req, res) => {
    res.send({ });
});


export default router;